/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Navecacao from './src/navegacao';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Navecacao);
