
/**
 * Recebe um tamanho e uma cor e retorna um objeto de estilo
 * para fins de debugs.
 */
const borderDebug = (width, color) => (
    {
        borderWidth: width,
        borderColor: color
    }
)
export { borderDebug }